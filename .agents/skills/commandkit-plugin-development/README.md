# commandkit-plugin-development skill

Self-contained skill for creating CommandKit runtime and compiler plugins.

Use this skill when:
- implementing `RuntimePlugin` hooks
- implementing `CompilerPlugin` transforms
- extending `commandkit create` templates
- wiring `rolldownPlugins` for compile-time customization

The skill includes detailed references and template generator tools for both plugin classes.
